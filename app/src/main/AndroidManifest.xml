<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.miss.dzikirharian1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Takbir" />
        <activity android:name=".Tasbih" />
        <activity android:name=".Tahmid" />
        <activity android:name=".DzikirSetelahShalat" />
        <activity android:name=".MenuDoa" />
        <activity android:name=".RecyclerviewAdapterDzikir" />
        <activity android:name=".DoaHarian" />
        <activity android:name=".DzikirPetang" >
                        <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".MenuDoa"/>
        </activity>
        <activity android:name=".DzikirPagi" />
        <activity android:name=".RecyclerViewAdapter" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
